# Chrome Web Store 掲載情報・提出ガイド

限定公開（Unlisted）で提出する前提の、掲載フォーム記入内容と手順のまとめ。
アップロードする zip は `npm run release` で生成される `release/webike_parts_bridge-v<version>.zip`。

---

## 1. 事前準備（一度だけ）

1. Chrome ウェブストア デベロッパー ダッシュボードに Google アカウントで登録: https://chrome.google.com/webstore/devconsole
2. **登録料 5 USD**（一回きり）を支払う。
3. プライバシーポリシーを URL で公開する（`PRIVACY.md` を利用）。例:
   - GitHub にこのリポジトリを置き、`PRIVACY.md` の raw URL、または GitHub Pages を使う。
   - どこかに1ページで掲載できれば可。

## 2. パッケージ生成

```
npm run release
```

→ `release/webike_parts_bridge-v0.1.0.zip` が出力される（manifest.json が zip 直下にあることを確認済み）。

## 3. ダッシュボードで新規アイテムを作成し zip をアップロード

## 4. ストアの掲載情報（コピペ用）

**アイテム名 (Name)**

```
【非公式】純正部品 品番ブリッジ (ヤマハ→Webike)
```

**概要 / Summary（132文字以内）**

```
ヤマハのパーツカタログで選んだ純正部品の品番・数量を、Webikeの純正部品注文フォームへワンクリックで転記。手入力の手間とミスを削減する非公式ツール。
```

**説明 / Description**

```
ヤマハ発動機のパーツカタログ（選択部品リスト）に集めた純正部品の「品番」「数量」を、
Webike の純正部品 見積・注文ページの入力フォームへ一括で転記する非公式の補助ツールです。

■ 使い方
1. ヤマハのパーツカタログで部品を選び「選択部品リスト」を表示し、画面右下の「選択部品を取込」を押す。
2. Webike の純正部品ページを開き、画面右下の「取込部品を転記」を押す。
3. 品番と数量が各行に入力されます。内容を確認し、送信は手動で行ってください。

■ 特長
・手入力の打ち間違いを防止
・拡張機能アイコンのポップアップで、取り込んだ部品の確認・数量編集・削除が可能
・自動送信はしません（誤発注防止）

■ プライバシー
・個人情報は収集しません。データは端末内にのみ保存し、外部送信は一切行いません。

■ 注意
本拡張は非公式ツールであり、ヤマハ発動機株式会社・株式会社ウェビックとは関係ありません。
対象サイトの仕様変更により動作しなくなる場合があります。
```

**カテゴリ (Category)**: ショッピング（Shopping） もしくは ユーティリティ／ツール
**言語 (Language)**: 日本語

## 5. 画像アセット

- **アイコン 128x128**: マニフェストに同梱済み（`icons/icon128.png`）。ストア用にも同画像を使用可。
- **スクリーンショット（必須・1枚以上、1280x800 または 640x400）**:
  すでに動作確認で撮った以下のような画面を使うと良い:
  1. ヤマハ選択部品リスト＋「選択部品を取込」ボタン＋取込結果のポップアップ
  2. Webike フォームに品番が転記された画面
     ※ 画面内に個人情報（氏名・住所・ログイン情報）が写り込まないよう注意。
- 小さいプロモタイル等は任意（限定公開では必須ではない）。

## 6. プライバシー関連の申告（Privacy タブ）

- **単一用途 (Single purpose)** の説明:
  ```
  ヤマハのパーツカタログで選択した純正部品の品番・数量を、Webikeの純正部品注文フォームへ転記する。
  ```
- **権限の正当性 (Permission justification)**:
  - `storage`:
    ```
    取り込んだ部品リスト（品番・数量）を、転記時に使うため端末内に一時保存する目的でのみ使用します。
    ```
  - ホスト権限（`*.yamaha-motor.co.jp` / `www.webike.net`）:
    ```
    ヤマハのパーツカタログから部品リストを読み取り、Webikeの注文フォームへ入力するために、
    この2サイトのページ上でのみコンテンツスクリプトを実行します。
    ```
- **リモートコード使用**: いいえ（すべてパッケージに同梱、外部スクリプト読込なし）。
- **データ収集**: 収集なし（下記すべて「収集しない」を選択）。
- **プライバシーポリシー URL**: 上記2で公開した URL を記入。
- 「ユーザーデータの取り扱いに関する開示」の各チェック（第三者提供なし・売買なし 等）に同意。

## 7. 公開設定

- **公開範囲**: 限定公開（Unlisted）を選択。
- 審査に提出。限定公開でも審査はあり、通常数時間〜数日。
- 公開後に発行される URL / インストールリンクを控える。

## 8. 更新時

1. `manifest.config.ts` の version（= `package.json` の version）を上げる。
2. `npm run release` で新しい zip を生成。
3. ダッシュボードの同一アイテムに新 zip をアップロードして再提出。

---

## 留意（審査リスク）

- 対象2サイトの商標名を名称に含むため、**「非公式」表記**で公式誤認を避けている（Unlisted も選択）。
- 他社サイトのフォームを自動入力するツールは、対象サイトの利用規約に抵触しないか各自で確認すること。
- 自動送信は実装していない（誤発注・スパム防止）。この設計は審査説明でも明示するとよい。
